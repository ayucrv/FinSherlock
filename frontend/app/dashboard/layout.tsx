import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Investigation Room - FinSherlock' }
export default function DashboardLayout({ children }: { children: React.ReactNode }) { return <>{children}</> }